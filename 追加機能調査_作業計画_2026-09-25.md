# Scan2FEM 追加機能調査・作業計画

| 項目 | 内容 |
| --- | --- |
| 調査日 | 2026-09-25 |
| 対象 | ローカルの `b0746b5` 時点のコード、`README.md`、`PROGRESS.md`、既存の作業計画、および Web・GitHub の一次資料 |
| 位置付け | [Webアプリ構築_作業計画.md](Webアプリ構築_作業計画.md)のフェーズ0を優先する方針を維持した追加機能計画。調査時点では実装・公開は行わない |

## 1. 調査結果と判断

現行アプリは画像・動画の取込、ブレ判定、EXIF保存、IndexedDBのプロジェクト管理、デモ形状の表示、2点スケール校正、PLY/STL/ZIP出力、PWAに対応している。一方、実画像からのSfM/MVS、サーフェス再構成、四面体メッシュ生成は[スタブ](app/src/pipeline/reconstructStub.ts)の段階である。「実画像からFEM向けメッシュを作る」経路は、既存の**フェーズ0技術検証が最優先**であり、本書の追加機能で完成済みとは扱わない。

今回、有効と判断した候補を次に示す。優先度はユーザー価値、現行コードとの接続、実装依存、端末負荷から判断した提案であり、外部資料が本アプリの性能を保証するものではない。

| ID | 候補 | 区分・優先度 | 規模目安 | 判断の理由 |
| --- | --- | --- | --- | --- |
| F1 | 再構成前の画像セット診断 | 新規・高 | 中 | 保存済みの寸法、ブレ評価、EXIF焦点距離を活用できる。COLMAPは十分な視点差・重なり・テクスチャを推奨し、OpenMVGは焦点距離の初期値が得られない場合の扱いを明記する。診断は撮り直し・採用画像の判断を支援する。[COLMAP](https://colmap.github.io/tutorial)、[OpenMVG](https://github.com/openMVG/openMVG/wiki/OpenMVG-on-your-image-dataset) |
| F2 | 外部PLY/STL形状の取込 | 新規・高 | 中〜大 | 実再構成の完成前でも実形状を既存ビューア、尺度校正、履歴、出力で確認できる。COLMAPは点群と面メッシュをPLYで出力し、導入済みのThree.jsにPLY/STLパーサがある。[COLMAP](https://colmap.github.io/cli.html)、[PLYLoader](https://threejs.org/docs/pages/PLYLoader.html)、[STLLoader](https://threejs.org/docs/pages/STLLoader.html) |
| F3 | 大容量ZIPの逐次入出力と読込制限 | 既存C-9の拡張・高 | 大 | 現在の[zip.ts](app/src/export/zip.ts)は各Blobの全読込、ZIPの全生成・全展開を行う。fflateは逐次APIを備える。読込側は展開後サイズがヘッダから得られない場合もあるため、実際に受けたバイト数で制限する。[fflate](https://github.com/101arrowz/fflate/blob/master/README.md)、[UnzipFile](https://github.com/101arrowz/fflate/blob/master/docs/interfaces/UnzipFile.md) |
| F4 | 表示専用の点群・メッシュ軽量化 | 既存計画R8の具体化・条件付き | 中 | 実データでビューアが重い場合に限り、元データと出力データを保持して表示用だけを間引く。meshoptimizerのJS/WASM実装は候補だが、導入前に端末上で効果とライセンスを確認する。[meshoptimizer.js](https://github.com/zeux/meshoptimizer/blob/master/js/README.md) |
| F5 | 大きな段階データのOPFS保存 | 既存C-10・条件付き | 大 | サイズ計測でIndexedDB保存・読込が律速と判明した場合に検証する。OPFSも同一オリジンの保存容量・消去規則に従うため、ZIPバックアップの代わりにはならない。[MDN: OPFS](https://developer.mozilla.org/en-US/docs/Web/API/StorageManager/getDirectory)、[MDN: 容量と消去](https://developer.mozilla.org/en-US/docs/Web/API/Storage_API/Storage_quotas_and_eviction_criteria) |

既存C-11のプロジェクト複製、C-12のスクリーンショット保存、フェーズ2の撮影方向ガイド、マーカーによる自動校正は有用だが、現時点ではフェーズ0・F1〜F3より後に置く。撮影方向の妥当性やマーカーの尺度精度は、実SfMの座標系と実機データで検証してから約束する。

## 2. 作業順序と受入条件

### 0. GitHub Actionsの利用制限を先に反映する

現在の `.github/workflows/ci.yml` は `main` へのpushとPR、`deploy-pages.yml` は `main` へのpushと手動操作で起動する。**開発中はGitHub Actionsを使わない**。次のリモートpushまたはPR作成より前に、リポジトリ設定で既存ワークフローとPages公開を停止し、両ワークフローファイルを撤去する。リポジトリ全体のActions停止を選ぶ場合は、最終公開時のPages動作に必要な設定変更を公開手順に記録する。[GitHub: Actions設定](https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-features/enabling-features-for-your-repository/managing-github-actions-settings-for-a-repository)、[GitHub: ワークフロー停止](https://docs.github.com/en/actions/managing-workflow-runs-and-deployments/managing-workflow-runs/disabling-and-enabling-a-workflow)

開発中の検証はローカルで `cd app && npm ci && npm run typecheck && npm run build` を実行し、変更した機能をブラウザ・必要な実機で確認する。`npm run build` には現行の依存ライセンス検査とPWA資源生成が含まれる。自動CIや自動デプロイを新設しない。

**最終公開時だけ**、ユーザーが許可したGitHub Pagesを使用する。ローカルで作った `app/dist` を手動でPages配信用ブランチへ配置し、Pagesをそのブランチから公開する方法を第一候補とする。GitHubの公式資料によると、ブランチ公開でもPages内部のActions実行は発生する。これは最終公開時だけの例外として扱い、通常のCI・既存の自動デプロイワークフローは復活させない。公開前にPWA、相対パス、`crossOriginIsolated`、オフライン起動を公開URLで確認する。[GitHub: Pages公開元](https://docs.github.com/en/pages/getting-started-with-github-pages/configuring-a-publishing-source-for-your-github-pages-site)

**受入条件:** 最終公開前のpush/PRで既存ワークフローが起動せず、検証記録がローカルコマンドと手動確認だけで残る。最終公開のPages実行は公開作業として明示される。

### 1. フェーズ0の実現性を先に判定する

[フェーズ0の検証表](docs/phase0/README.md)に沿って、実画像、CAD正解形状、Android端末、PCでOpenMVG→自前MVS→PoissonRecon→Manifold→fTetWildの可否、品質、処理時間、ピークメモリを記録する。各候補の版・ビルド設定・入力ハッシュ・ライセンスも記録する。fTetWildの開発元は複雑なモデルで32GB超のメモリ使用例を挙げ、ManifoldはEmscriptenでの並列ビルドを推奨していないため、Androidでの完走や並列化を未検証のまま前提にしない。[fTetWild](https://github.com/wildmeshing/fTetWild)、[Manifold](https://github.com/elalish/manifold)

**判定点:** 画像枚数・解像度・形状複雑度ごとの上限と、スマホ単独処理またはPCブラウザ継続の運用を実測で決める。未達なら原因と代替案を`docs/phase0/`へ記録する。F4/F5の着手判断もこの測定値に基づける。

### 2. F1: 画像セット診断を実装する

1. [PipelinePanel.tsx](app/src/pipeline/PipelinePanel.tsx)の「採用枚数」だけの表示を、採用・除外枚数、寸法の分布、ブレ評価、焦点距離候補の有無、カメラ機種の混在を示す一覧へ拡張する。既存の[types.ts](app/src/types.ts)の `AssetMeta.image` と `quality` を使う。
2. 原画を一括読込せず、既存サムネイルから露出・類似画像の**候補**を段階的に調べる。類似だけで自動除外せず、判定理由と該当画像への導線を[Gallery.tsx](app/src/capture/Gallery.tsx)に示す。重なりや3D復元可能性は、特徴点マッチング前には断定しない。
3. 焦点距離候補がない画像には、後続SfMで確認・補正する入力欄を設計する。推定値を自動で「正解」と扱わず、入力値の単位と根拠を記録する。

**受入条件:** EXIF欠損、混在解像度、ブレ画像、近似重複を含む検証セットで警告が再現し、表示する画像数・対象IDが正しい。診断だけで画像・段階データが削除または上書きされない。日英表示を確認する。

### 3. F2: 外部形状取込を実装する

1. 最初はPLY点群、PLY三角面、ASCII/binary STLに限定する。Three.jsの既存アドオンで読み、頂点の有限値、三角面の索引、空データ、ファイルサイズ・頂点数の上限を検証してから保存する。上限値はフェーズ0または代表的な端末での試験値から決める。
2. [Stage](app/src/types.ts)に「合成デモ／本アプリの撮影再構成／外部取込」の由来を識別できる情報を追加し、旧データの `demo` 属性と両立させる。外部形状は新規の履歴として追記し、ビューアと[ExportPanel.tsx](app/src/ui/ExportPanel.tsx)でも由来を表示する。
3. STL/PLYの入力単位は利用者に確認する。特にSTLファイルは単位を明示しない。[ADMeshの形式説明](https://admesh.readthedocs.io/_/downloads/en/latest/pdf/) 取込形状の尺度は既存の由来付き校正に結び、別の再構成系列へ流用しない。ZIPのマニフェスト更新時は旧版ZIPの復元も維持する。

**受入条件:** PLY点群・PLY面メッシュ・ASCII/binary STLを取込み、再読込・ZIP移動後も形状、単位、由来、尺度が一致する。不正形状・容量超過は保存前に拒否し、プロジェクトに部分データを残さない。外部取込を「本アプリによる実画像再構成」や「検証済みFEMモデル」と表示しない。

### 4. F3: ZIPを大容量でも安全に扱う

1. まず出力側を[fflateの逐次ZIP](https://github.com/101arrowz/fflate/blob/master/README.md)と `Blob.stream()` に置き換え、アセット単位または小さなチャンクで生成する。対応ブラウザでは `showSaveFilePicker` による直接保存を検証する。非対応環境には従来方式を残し、実測で決めたサイズ以上では無理に一括生成しない。[MDN: Blob.stream](https://developer.mozilla.org/en-US/docs/Web/API/Blob/stream)、[MDN: 保存ダイアログの対応制限](https://developer.mozilla.org/en-US/docs/Web/API/Window/showSaveFilePicker)
2. 読込側は `fflate.Unzip` でエントリ名の許可リスト、件数、各エントリと全体の**実際の展開済みバイト数**を制限する。マニフェスト、参照、サイズを検証し終えるまでプロジェクトを確定しない。大容量ファイルの一時置場と失敗時清掃は、OPFSを含めて小さな実証で決める。
3. 中断、ブラウザ再読込、空き容量不足、壊れたZIP、欠損アセットの挙動を確認する。実行中ジョブのcheckpointをZIPへ含めない既存仕様は維持する。

**受入条件:** 小容量ZIPの旧形式との相互取込が通り、代表サイズで現行方式よりピークメモリを下げられる。制限超過・欠損・壊れたZIPは明示エラーとなり、プロジェクトと一時ファイルに残留物がない。

### 5. F4/F5は測定結果で着手を決める

- **F4:** 実PLY/メッシュでビューア操作が遅い場合、Workerで表示専用の点間引き・面簡略化を評価する。履歴に保存した元データとPLY/STL/将来のFEM出力は変更せず、元データのハッシュ一致と操作速度の改善で判定する。
- **F5:** IndexedDBの大きなBlobの読書きが律速なら、OPFSへ一時ファイル→検証→参照メタ更新の順で移す実証を行う。失敗・キャンセル・プロジェクト削除後の孤児ファイル清掃、旧DBとZIPの互換性を確認する。容量超過やブラウザデータ消去への対策はZIPバックアップを維持する。

## 3. 実施マイルストーン

| 順 | 成果物 | 完了判定 |
| --- | --- | --- |
| M0 | Actions停止とローカル検証手順 | 次のpush/PRより前に既存ワークフローの起動を停止。公開前の自動実行なし |
| M1 | フェーズ0の実機・実画像測定 | 実現可能な上限と未達項目を`docs/phase0/`に記録 |
| M2 | F1 画像セット診断 | 撮り直し・採用判断に使える診断と検証セットの結果 |
| M3 | F2 外部PLY/STL取込 | 実形状の表示・尺度・履歴・ZIP移動が成立 |
| M4 | F3 大容量ZIP | 逐次出力と制限付き読込の成功、失敗時の残留なし |
| M5 | F4/F5の採否決定 | 実データの速度・メモリ測定に基づいて着手または見送りを記録 |
| M6 | 最終GitHub Pages公開 | ローカルビルド済み成果物を手動で公開し、公開URLで動作確認。Pages内部実行のみ最終公開の例外 |

各実装マイルストーンで`PROGRESS.md`、READMEの日英の機能表、アプリ内の「デモ／外部取込／実再構成」表示を同期する。新しい本番依存を採用する場合は既存の`licenses:generate`と`npm run build`で配布条件を確認する。

## 4. 調査の限界

Web・GitHub資料はAPIや他製品の実装可能性を示すもので、Scan2FEMでの速度・精度を証明しない。実カメラ、Android端末、CAD正解形状を使う試験は未実施であり、フェーズ0の結果で優先順位とファイル上限を見直す。既存の`.github/workflows/`はこの調査では変更していないため、リモート作業の前にM0を実施する。
