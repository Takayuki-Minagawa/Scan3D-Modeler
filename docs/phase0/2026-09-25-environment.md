# 2026-09-25 フェーズ0環境確認

追加機能実装の開始時に `./scripts/check-phase0-toolchain.sh` を実行した。Node.js 22.18.0、npm 10.9.3、Git 2.54.0、CMake 4.4.3 は利用可能。`emcc` と `emcmake` は見つからず、Emscriptenの固定版ビルドは実行できていない。`adb` も見つからず、接続済みAndroid端末の検査はできていない。

20〜60枚の実撮影画像、CAD正解形状、Android中位機の所在はこの作業環境から確認できていない。0-3〜0-5のWASMビルド、0-7の端末別上限、0-8のCAD比較の実測値は**未取得**である。デモ形状や外部取込形状をその代用値とはしない。

フェーズ0の合否は未判定。F4の表示軽量化とF5のOPFS移行も、代表的な実データで操作速度・メモリ・IndexedDB読書きの律速を計測してから採否を決める。追加した外部形状の32MiB/100万頂点/100万三角形上限は安全のための暫定値であり、実機性能の保証値ではない。

再開時はEmscriptenの版とリビジョンを固定し、[README](README.md)の検証表に沿って、入力ハッシュ、ライセンス、ビルド設定、端末、ブラウザ、処理時間、ピークメモリ、品質を同じ日付の記録へ追記する。
